// Sitewide launch switch. Flip to false once the site is verified and ready
// to be publicly discovered/indexed. Consumed by the root layout (meta
// robots tag) and robots.ts (robots.txt) so both stay in sync.
export const SITE_IS_PRIVATE = true;
